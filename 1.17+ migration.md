# 1.17+ migration idk cat

NeoForge primers (they use Mojmap so use https://mappings.dev):
<https://docs.neoforged.net/primer/docs/1.15/#porting> (has docs on push matrix / pop matrix)
<https://docs.neoforged.net/primer/docs/1.17/>
<https://docs.neoforged.net/primer/docs/1.19.3/>
<https://docs.neoforged.net/primer/docs/1.19.4/>

## 1.16.5 -> 1.17

`color4f` -> `setShaderColor`

## Pushing & Popping

```kotlin
RenderSystem.pushMatrix()

// do scruffy stuff with `RenderSystem`

// then pop
RenderSystem.popMatrix()

```

```kotlin
// Push
val matrixStack = RenderSystem.getModelViewStack()
matrixStack.push()

// do scruffy stuff on `matrixStack`

// then pop

val matrixStack = RenderSystem.getModelViewStack()
matrixStack.pop()
RenderSystem.applyModelViewMatrix()
```
