package com.skidders.sigma.util.client.interfaces;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public interface ILogger {
    Logger logger = LogManager.getLogger("Sigma");
}
